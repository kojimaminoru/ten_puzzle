#include <iostream>
#include <vector>
#include <string.h>
using namespace std;
#define NUMS 4
#define TARGET 10

int main(int argc, const char * argv[]) {
    int a;
    vector<int> num(NUMS);
    cin >> a;
    cout << a << endl;
    return 0;
}
